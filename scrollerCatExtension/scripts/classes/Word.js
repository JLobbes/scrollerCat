// scripts/classes/Word.js

(function () {
    class Word {
        constructor(text, width) {
            this.text = text;
            this.width = width;
        }
    }

    window.Word = Word;
})();
